###############################################################################
# scratch.aog
# 
# Scratchpad for testing AOG expressions.

$Document = DocScan(
  (
    "text" => "Text"
  )
);


#$Time3 = $Document;
#Regex(/the/, (0 => "match"), "text", $Document);

#$TimeAll = $Document;
#   Regex(/the/, (0 => "match"),"text", $Document);

$TimeMinus =
Difference(
    $Document,
    $Document
);
