-- Test to verify if Document schema is not null in metadata and that it matches with expected

module documentMetadataTest;

require document with columns
	field1 Text and
	field2 Integer and
	field3 Text;