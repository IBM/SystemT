/*
   Test to verify, that the loader throws an exception, when a null ETI is passed and 
   there are required tables in the module.
*/

module extTableNullETITest;

-- create external table - cannot be empty
create external table extTab2(id2 Integer, text2 Text)
required true;
