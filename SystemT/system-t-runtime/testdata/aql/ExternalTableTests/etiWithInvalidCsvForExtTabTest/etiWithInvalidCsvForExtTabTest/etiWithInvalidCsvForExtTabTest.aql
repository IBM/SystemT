/*
Test to verify, that the loader throws an exception, when the passed ETI contains an invalid CSV file
as a table.
*/
module etiWithInvalidCsvForExtTabTest;

-- create external table - can be empty
create external table extTab1(id Integer, text Text)
allow_empty true;
