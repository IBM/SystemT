-- this module is the base module that exports elements for other modules to import
module module1;

create view foo as 
select D.text as match from Document D;
export view foo;

create view bar as
select D.text as match from Document D;
export view bar;