-- Test case to verify that the parser throws and error for 'set default dictionary ...' statement; when in backward
-- compatibility mode.
set default dictionary language as 'en,fr';

create dictionary testDict 
as
(
	'firstEntry','secondEntry'
);

