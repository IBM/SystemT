-- Test to assert that the compiler returns error for language code string containing invalid/unsupported languages.
set default dictionary language as 'english,fr,german';

create dictionary testDict 
as
(
	'firstEntry','secondEntry'
);

