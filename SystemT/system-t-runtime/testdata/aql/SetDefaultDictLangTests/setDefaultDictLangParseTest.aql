-- Test to assert that the parser returns parse tree node for 'set default dictionary language ...' statement.
set default dictionary language as 'en,fr';

create dictionary testDict 
as
(
	'firstEntry','secondEntry'
);

