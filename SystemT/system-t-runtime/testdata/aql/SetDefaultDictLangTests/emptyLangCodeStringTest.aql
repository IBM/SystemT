-- Test to assert that the compiler return error for empty language code string.
-- language code string containing whitespaces
set default dictionary language as '  ';

create dictionary testDict 
as
(
	'firstEntry','secondEntry'
);

