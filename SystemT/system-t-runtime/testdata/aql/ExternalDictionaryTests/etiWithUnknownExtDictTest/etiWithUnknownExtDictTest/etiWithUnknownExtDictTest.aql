/*
Test to verify, that the loader throw an exception, when the passed ETI contain dictionaries, which 
are either *not declared* or *not declared external* in the loaded module
*/
module etiWithUnknownExtDictTest;

-- create required external dictionary
create external dictionary requiredExtDict
allow_empty true
with language as 'en'
and case exact;
