require document with columns text Text;

--
-- Test case for defect #13079 - SystemT.Single.getExternalViewSchema() fails in certain cases 
--


-- Declare an external view which does not contribute to any output
-- to make sure we can still populate it at runtime
create external view Word(match Span) 
external_name 'WordExternal'; 









